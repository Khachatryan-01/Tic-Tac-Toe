class Title extends Base {
    constructor(container, text, className) {
        super({
            tagName: "h2",
            text,
            className,
            container
        });

    }
}
