class Title extends Base {
    constructor({ text, className, container }) {
        super({
            tagName: "h2",
            text,
            className,
            container
        });

    }
}